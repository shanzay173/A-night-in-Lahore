/**
 * Grain — the barely-there film grain that lives above everything.
 * Rendered once in Layout; the noise + animation live in index.css.
 */
export default function Grain() {
  return <div className="grain" aria-hidden="true" />
}